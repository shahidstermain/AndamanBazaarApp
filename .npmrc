# Suppress spurious peer-dependency audit warnings from vite-plugin-pwa.
# vite-plugin-pwa@1.x declares a peer range of vite@^3-5 but works correctly
# with vite@5.x. The override below satisfies the peer check without
# repo-wide legacy resolution.
